<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:adonis="clr-namespace:AdonisUI;assembly=AdonisUI"
    xmlns:adonisEx="clr-namespace:AdonisUI.Extensions;assembly=AdonisUI"
    xmlns:local="clr-namespace:MyHealth">
    <Style TargetType="local:TimerControl">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:TimerControl">
                    <TextBlock Style="{StaticResource Texts.SmallTitle}" Text="{TemplateBinding Remained, Converter={StaticResource TimeSpan2StringFormatter}}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
